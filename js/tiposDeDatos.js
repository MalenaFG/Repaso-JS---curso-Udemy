//Tipos de datos que no conocía

//comando para reorganizar lineas largas alt+z

//Symbol: todos son únicos nunca se repite uno, si los comparas, aunque tengan el mismo valor, parecerán false, siempre son diferentes aunque tengan el mismo valor asignado

const firstSymbol = Symbol(30)
const secondSymbol = Symbol(30)
console.log(firstSymbol == secondSymbol)
